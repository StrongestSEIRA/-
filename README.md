# 飲酒記録アプリ（フロントエンド）

このプロジェクトはブラウザで動作するシンプルな飲酒記録アプリです。

## 開発サーバー起動手順

```bash
npm install
npm run dev
```

`npm run dev` 実行後に表示されるURLをブラウザで開くと利用できます。

## 補足

- UIレイアウトは `index.html` のまま維持しています。
- 依存は `vite` のみです。
